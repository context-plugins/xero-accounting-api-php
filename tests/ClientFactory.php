<?php

declare(strict_types=1);

/*
 * XeroAccountingAPILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace XeroAccountingAPILib\Tests;

use Core\Types\CallbackCatcher;
use XeroAccountingAPILib\Authentication\AuthorizationCodeAuthCredentialsBuilder;
use XeroAccountingAPILib\XeroAccountingAPIClient;
use XeroAccountingAPILib\XeroAccountingAPIClientBuilder;

class ClientFactory
{
    public static function create(CallbackCatcher $httpCallback): XeroAccountingAPIClient
    {
        $clientBuilder = XeroAccountingAPIClientBuilder::init();
        $clientBuilder = self::addConfigurationFromEnvironment($clientBuilder);
        $clientBuilder = self::addTestConfiguration($clientBuilder);
        return $clientBuilder->httpCallback($httpCallback)->build();
    }

    public static function addTestConfiguration(
        XeroAccountingAPIClientBuilder $builder
    ): XeroAccountingAPIClientBuilder {
        return $builder;
    }

    public static function addConfigurationFromEnvironment(
        XeroAccountingAPIClientBuilder $builder
    ): XeroAccountingAPIClientBuilder {
        $timeout = getenv('XERO_ACCOUNTING_API_LIB_TIMEOUT');
        $numberOfRetries = getenv('XERO_ACCOUNTING_API_LIB_NUMBER_OF_RETRIES');
        $maximumRetryWaitTime = getenv('XERO_ACCOUNTING_API_LIB_MAXIMUM_RETRY_WAIT_TIME');
        $environment = getenv('XERO_ACCOUNTING_API_LIB_ENVIRONMENT');
        $oAuthClientId = getenv('XERO_ACCOUNTING_API_LIB_O_AUTH_CLIENT_ID');
        $oAuthClientSecret = getenv('XERO_ACCOUNTING_API_LIB_O_AUTH_CLIENT_SECRET');
        $oAuthRedirectUri = getenv('XERO_ACCOUNTING_API_LIB_O_AUTH_REDIRECT_URI');

        if (!empty($timeout) && \is_numeric($timeout)) {
            $builder->timeout(intval($timeout));
        }

        if (!empty($numberOfRetries) && \is_numeric($numberOfRetries)) {
            $builder->numberOfRetries(intval($numberOfRetries));
        }

        if (!empty($maximumRetryWaitTime) && \is_numeric($maximumRetryWaitTime)) {
            $builder->maximumRetryWaitTime(intval($maximumRetryWaitTime));
        }

        if (!empty($environment)) {
            $builder->environment($environment);
        }

        if (!empty($oAuthClientId) && !empty($oAuthClientSecret) && !empty($oAuthRedirectUri)) {
            $builder->authorizationCodeAuthCredentials(
                AuthorizationCodeAuthCredentialsBuilder::init(
                    $oAuthClientId,
                    $oAuthClientSecret,
                    $oAuthRedirectUri
                )
            );
        }

        return $builder;
    }
}
